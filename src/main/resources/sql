CREATE TABLE items (
                       id INTEGER PRIMARY KEY AUTOINCREMENT,
                       name TEXT,
                       price REAL,
                       purchase_time DATETIME
);
